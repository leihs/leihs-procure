#!/usr/bin/env bash
set -euo pipefail

PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd ../.. > /dev/null 2>&1 && pwd -P)"
CLJ_DIR="${PROJECT_DIR}/server"
source ${CLJ_DIR}/shared-clj/bin/helpers/clj.sh
PROJECT_NAME=leihs-procure clj_watson "$@"

# vi: ft=sh
