task_defaults:
  traits:
    Docker Engine: true

  environment_variables:
    CI: 'true'

    LEIHS_ADMIN_DIR: '{{CIDER_CI_WORKING_DIR}}'
    LEIHS_DATABASE_DIR: '{{LEIHS_ADMIN_DIR}}/database'

    DIST_IMAGE_NAME: 'leihs-admin'
    DIST_VERSION_NAME: 'trial-{{CIDER_CI_TRIAL_ID}}'
